pre-commit:
  commands:
    check:
      glob: "*.go"
      run: make check

pre-push:
  commands:
    build:
      run: go build ./...
    coverage-check:
      run: make coverage-check
    e2e:
      run: make e2e
